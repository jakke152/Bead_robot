To use simulator run main.exe in the same folder as SDL2.dll, hopefully it'll work.
To be able to compile it you need to install SDL2 and link it to your compiler.
The simulation will run differently depending on how quickly your pc runs it, so timings will probably be wrong.
